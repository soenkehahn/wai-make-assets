name: wai-make

dependencies:
  - base
  - wai-app-static
  - wai
  - warp
  - string-conversions
  - bytestring
  - shake
  - http-types

executables:
  server:
    main: Main.hs
    source-dirs:
      - src

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
      - wreq
      - mockery
      - lens
      - directory
